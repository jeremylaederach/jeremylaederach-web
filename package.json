{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "build:static": "npm run build && npm run export:static",
        "dev": "vite",
        "export:static": "php artisan site:export-static"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "simple-icons": "^16.26.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
